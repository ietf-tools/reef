{% comment %}
One sentence naming what a subscription covers, for a message about exactly one:
the confirmation. Include it with a lead, which is the words before the noun phrase.

    {% include "subscriptions/mail/_subscription_line.txt" with lead="You are now subscribed to" %}

Context: subscription, watched_doc (the rfc kind's document, in prose form), and
lead. The phrase itself lives in _subscription_phrase.txt, shared with the digest,
which lists several. The whole sentence is wrapped rather than the phrase, because a
set title is up to 200 characters of the owner's choosing and cannot be trusted to
fit a line.
{% endcomment %}{% autoescape off %}{% filter wordwrap:78 %}{{ lead }} {% include "subscriptions/mail/_subscription_phrase.txt" %}.{% endfilter %}{% endautoescape %}
