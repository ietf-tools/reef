openapi: 3.0.3
info:
  title: Reef
  version: 0.1.0
  description: Backend API for the Reef survey and engagement service
paths:
  /api/reef/me/documents/:
    get:
      operationId: me_documents_retrieve
      description: |-
        Return the authenticated caller's rating, subscription and set membership for each named document, along with the caller's own sets. One call for a whole page of documents.

        `doc` is repeatable and identifiers are canonicalized, so `rfc9110` and `RFC 9110` address the same document; the series has to be named, so `9110` on its own is rejected. A named document is always returned, with nulls if the caller has no state for it. At most 100 documents per request.

        Naming no document returns the caller's sets and an empty `documents` list, which is how a client loads the set list on its own. Unlike `/stats/`, an empty `doc` does not mean every document: that endpoint is swept once at build time, whereas this one is called on every page.

        Carries no public numbers. Ratings averages, subscriber counts and set counts are the same for every visitor and are served by `/stats/`.
      summary: Read your own state for a batch of documents
      parameters:
      - in: query
        name: doc
        schema:
          type: array
          items:
            type: string
        description: 'Document identifier, repeatable, at most 100 per request. The
          series must be named: rfc9110, bcp14, std66.'
      tags:
      - me
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MyDocuments'
          description: ''
  /api/reef/popularity/:
    get:
      operationId: popularity_list
      description: The curated most-popular list. Public; consumed by Red at build
        time.
      tags:
      - popularity
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PopularEntry'
          description: ''
  /api/reef/ratings/{rfc}/:
    get:
      operationId: ratings_retrieve
      description: |-
        Return the public average and count of ratings for one RFC. Open to anonymous callers. A credential adds nothing but `your_rating`, the caller's own 1-5 rating of this RFC, which is null if they have not rated it; for an anonymous caller it is always null.

        The identifier is canonicalized, so `9110`, `rfc9110` and `RFC 9110` all address the same document.
      summary: Read an RFC's rating
      parameters:
      - in: path
        name: rfc
        schema:
          type: string
        required: true
      tags:
      - ratings
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingAggregate'
          description: ''
    put:
      operationId: ratings_update
      description: Record the authenticated caller's 1-5 rating of one RFC, replacing
        their previous rating of it if there is one. Requires a credential. Returns
        the same body as GET, so the response carries the recomputed average and count
        along with `your_rating` echoing the value just set.
      summary: Set the caller's rating of an RFC
      parameters:
      - in: path
        name: rfc
        schema:
          type: string
        required: true
      tags:
      - ratings
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RatingWrite'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/RatingWrite'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/RatingWrite'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingAggregate'
          description: ''
    delete:
      operationId: ratings_destroy
      description: 'Remove the authenticated caller''s rating of one RFC, so it no
        longer counts towards the average. Requires a credential. Idempotent: a caller
        who has not rated this RFC gets the same response as one whose rating was
        just removed. Returns the same body as GET, so the response carries the recomputed
        average and count, with `your_rating` now null.'
      summary: Withdraw the caller's rating of an RFC
      parameters:
      - in: path
        name: rfc
        schema:
          type: string
        required: true
      tags:
      - ratings
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RatingAggregate'
          description: ''
  /api/reef/schema/:
    get:
      operationId: schema_retrieve
      description: |-
        OpenApi3 schema for this API. Format can be selected via content negotiation.

        - YAML: application/vnd.oai.openapi
        - JSON: application/vnd.oai.openapi+json
      parameters:
      - in: query
        name: format
        schema:
          type: string
          enum:
          - json
          - yaml
      - in: query
        name: lang
        schema:
          type: string
          enum:
          - af
          - ar
          - ar-dz
          - ast
          - az
          - be
          - bg
          - bn
          - br
          - bs
          - ca
          - ckb
          - cs
          - cy
          - da
          - de
          - dsb
          - el
          - en
          - en-au
          - en-gb
          - eo
          - es
          - es-ar
          - es-co
          - es-mx
          - es-ni
          - es-ve
          - et
          - eu
          - fa
          - fi
          - fr
          - fy
          - ga
          - gd
          - gl
          - he
          - hi
          - hr
          - hsb
          - hu
          - hy
          - ia
          - id
          - ig
          - io
          - is
          - it
          - ja
          - ka
          - kab
          - kk
          - km
          - kn
          - ko
          - ky
          - lb
          - lt
          - lv
          - mk
          - ml
          - mn
          - mr
          - ms
          - my
          - nb
          - ne
          - nl
          - nn
          - os
          - pa
          - pl
          - pt
          - pt-br
          - ro
          - ru
          - sk
          - sl
          - sq
          - sr
          - sr-latn
          - sv
          - sw
          - ta
          - te
          - tg
          - th
          - tk
          - tr
          - tt
          - udm
          - ug
          - uk
          - ur
          - uz
          - vi
          - zh-hans
          - zh-hant
      tags:
      - schema
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/vnd.oai.openapi:
              schema:
                type: object
                additionalProperties: {}
            application/yaml:
              schema:
                type: object
                additionalProperties: {}
            application/vnd.oai.openapi+json:
              schema:
                type: object
                additionalProperties: {}
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: ''
  /api/reef/sets/:
    get:
      operationId: sets_list
      description: List and create the caller's document sets.
      tags:
      - sets
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DocumentSet'
          description: ''
    post:
      operationId: sets_create
      description: List and create the caller's document sets.
      tags:
      - sets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentSet'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DocumentSet'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DocumentSet'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
  /api/reef/sets/{id}/:
    get:
      operationId: sets_retrieve
      description: |-
        Read a set; retitle, redescribe or delete your own.

        One URL for a set, whoever is asking: the id is the whole of a set's
        identity, so a shared link is this link and there is no second read
        endpoint to keep in step with it. Reading needs no token, which is what
        makes the link shareable, and holding the id is the whole of the
        permission: a set is a thing its owner made to be passed around, and the
        id is unguessable so that passing it around is the only way in. Writing is
        the owner's alone, and a write to somebody else's set 404s rather than 403s
        so that the refusal says nothing about whose it is.

        A set staff have taken down 404s here too, for everyone alike: it is left
        out of the queryset rather than refused, so nothing confirms it exists.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
    put:
      operationId: sets_update
      description: |-
        Read a set; retitle, redescribe or delete your own.

        One URL for a set, whoever is asking: the id is the whole of a set's
        identity, so a shared link is this link and there is no second read
        endpoint to keep in step with it. Reading needs no token, which is what
        makes the link shareable, and holding the id is the whole of the
        permission: a set is a thing its owner made to be passed around, and the
        id is unguessable so that passing it around is the only way in. Writing is
        the owner's alone, and a write to somebody else's set 404s rather than 403s
        so that the refusal says nothing about whose it is.

        A set staff have taken down 404s here too, for everyone alike: it is left
        out of the queryset rather than refused, so nothing confirms it exists.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentSet'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DocumentSet'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DocumentSet'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
    patch:
      operationId: sets_partial_update
      description: |-
        Read a set; retitle, redescribe or delete your own.

        One URL for a set, whoever is asking: the id is the whole of a set's
        identity, so a shared link is this link and there is no second read
        endpoint to keep in step with it. Reading needs no token, which is what
        makes the link shareable, and holding the id is the whole of the
        permission: a set is a thing its owner made to be passed around, and the
        id is unguessable so that passing it around is the only way in. Writing is
        the owner's alone, and a write to somebody else's set 404s rather than 403s
        so that the refusal says nothing about whose it is.

        A set staff have taken down 404s here too, for everyone alike: it is left
        out of the queryset rather than refused, so nothing confirms it exists.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedDocumentSet'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedDocumentSet'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedDocumentSet'
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
    delete:
      operationId: sets_destroy
      description: |-
        Read a set; retitle, redescribe or delete your own.

        One URL for a set, whoever is asking: the id is the whole of a set's
        identity, so a shared link is this link and there is no second read
        endpoint to keep in step with it. Reading needs no token, which is what
        makes the link shareable, and holding the id is the whole of the
        permission: a set is a thing its owner made to be passed around, and the
        id is unguessable so that passing it around is the only way in. Writing is
        the owner's alone, and a write to somebody else's set 404s rather than 403s
        so that the refusal says nothing about whose it is.

        A set staff have taken down 404s here too, for everyone alike: it is left
        out of the queryset rather than refused, so nothing confirms it exists.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
  /api/reef/sets/{id}/documents/{doc}/:
    put:
      operationId: sets_documents_update
      description: |-
        Add or remove one document.

        PUT is idempotent, and the identifier is canonicalized first, so
        .../documents/RFC%209110/ and .../documents/rfc9110/ are the same entry.
      parameters:
      - in: path
        name: doc
        schema:
          type: string
        required: true
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
    delete:
      operationId: sets_documents_destroy
      description: |-
        Add or remove one document.

        PUT is idempotent, and the identifier is canonicalized first, so
        .../documents/RFC%209110/ and .../documents/rfc9110/ are the same entry.
      parameters:
      - in: path
        name: doc
        schema:
          type: string
        required: true
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
  /api/reef/sets/{id}/order/:
    put:
      operationId: sets_order_update
      description: |-
        Rewrite the display order in one request.

        Ranks are replaced as a block rather than patched per entry, so a
        drag-and-drop is one call that cannot half-apply.
      parameters:
      - in: path
        name: id
        schema:
          type: string
          format: uuid
        required: true
      tags:
      - sets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DocumentSetOrder'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/DocumentSetOrder'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/DocumentSetOrder'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DocumentSet'
          description: ''
  /api/reef/stats/:
    get:
      operationId: stats_list
      description: |-
        Rating, subscriber and set numbers per document.

        Public and unpaginated: Red precomputes the whole series in one call at
        build time, which is too many identifiers to name in a query string.
        Filtering with doc is for one-off lookups.
      parameters:
      - in: query
        name: doc
        schema:
          type: array
          items:
            type: string
        description: Document identifier, repeatable. Omit to get every document that
          has any engagement at all. A named document is always returned, with zeros
          if it has none.
      - in: query
        name: set
        schema:
          type: string
          format: uuid
        description: Document set id. Returns a row per document the set holds, including
          members with no engagement. Any set resolves for any caller holding its
          id; an id that names no set 404s. Combines with doc as an intersection.
      tags:
      - stats
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/DocumentStats'
          description: ''
  /api/reef/subjects/:
    get:
      operationId: subjects_list
      description: |-
        Every subject that exists, in name order. Public and unpaginated: the vocabulary is curated by staff rather than self-served, so it stays small enough to hand over whole.

        `doc` narrows the list to the subjects carried by one document, which is how a caller renders the subjects on an RFC page. The identifier is canonicalized, so `rfc9110` and `RFC 9110` address the same document, and the series has to be named.
      summary: List the subject vocabulary
      parameters:
      - in: query
        name: doc
        schema:
          type: string
        description: 'Return only the subjects carried by this document. The series
          must be named: rfc9110, bcp14, std66.'
      tags:
      - subjects
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Subject'
          description: ''
  /api/reef/subjects/{slug}/:
    get:
      operationId: subjects_retrieve
      description: 'Two shapes, told apart by `retired`. A live subject comes back
        in full. A retired one comes back as `slug`, `retired` and `merged_into` only:
        it is no longer offered and should not be rendered as current, and what is
        left is enough to redirect a link that names it.'
      summary: Read one subject and the documents carrying it
      parameters:
      - in: path
        name: slug
        schema:
          type: string
        required: true
      tags:
      - subjects
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SubjectDetailOrRedirect'
          description: ''
  /api/reef/subscriptions/:
    get:
      operationId: subscriptions_list
      description: List and create the current user's subscriptions.
      tags:
      - subscriptions
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Subscription'
          description: ''
    post:
      operationId: subscriptions_create
      description: List and create the current user's subscriptions.
      tags:
      - subscriptions
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Subscription'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Subscription'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Subscription'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Subscription'
          description: ''
  /api/reef/subscriptions/{id}/:
    delete:
      operationId: subscriptions_destroy
      description: Delete one of the current user's subscriptions.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - subscriptions
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
  /api/reef/surveys/:
    get:
      operationId: surveys_list
      description: List and create surveys. Staff only; used by the builder.
      tags:
      - surveys
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Survey'
          description: ''
    post:
      operationId: surveys_create
      description: List and create surveys. Staff only; used by the builder.
      tags:
      - surveys
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Survey'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Survey'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Survey'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Survey'
          description: ''
  /api/reef/surveys/{id}/:
    get:
      operationId: surveys_retrieve
      description: Retrieve, update, or delete a survey. Staff only; used by the builder.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - surveys
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Survey'
          description: ''
    put:
      operationId: surveys_update
      description: Retrieve, update, or delete a survey. Staff only; used by the builder.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - surveys
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Survey'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/Survey'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/Survey'
        required: true
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Survey'
          description: ''
    patch:
      operationId: surveys_partial_update
      description: Retrieve, update, or delete a survey. Staff only; used by the builder.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - surveys
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchedSurvey'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/PatchedSurvey'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/PatchedSurvey'
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Survey'
          description: ''
    delete:
      operationId: surveys_destroy
      description: Retrieve, update, or delete a survey. Staff only; used by the builder.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - surveys
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '204':
          description: No response body
  /api/reef/surveys/{id}/results/:
    get:
      operationId: surveys_results_retrieve
      description: Aggregated results feeding the analytics dashboard. Staff only.
      parameters:
      - in: path
        name: id
        schema:
          type: integer
        required: true
      tags:
      - surveys
      security:
      - BearerAuth: []
      - cookieAuth: []
      responses:
        '200':
          content:
            application/json:
              schema:
                type: object
                additionalProperties: {}
          description: ''
  /api/reef/surveys/{slug}/definition/:
    get:
      operationId: surveys_definition_retrieve
      description: |-
        Definition and theme for the runner. Published surveys only; an
        authenticated-visibility survey requires a signed-in caller.
      parameters:
      - in: path
        name: slug
        schema:
          type: string
        required: true
      tags:
      - surveys
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SurveyDefinition'
          description: ''
  /api/reef/surveys/{slug}/responses/:
    post:
      operationId: surveys_responses_create
      description: Submit a response to a published survey.
      parameters:
      - in: path
        name: slug
        schema:
          type: string
        required: true
      tags:
      - surveys
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ResponseCreate'
          application/x-www-form-urlencoded:
            schema:
              $ref: '#/components/schemas/ResponseCreate'
          multipart/form-data:
            schema:
              $ref: '#/components/schemas/ResponseCreate'
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '201':
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ResponseCreate'
          description: ''
  /api/reef/surveys/open/:
    get:
      operationId: surveys_open_list
      description: |-
        Open surveys Red may offer. Bearer optional: an identified user also
        receives their targeted surveys, an anonymous caller sees open ones only.
      tags:
      - surveys
      security:
      - BearerAuth: []
      - cookieAuth: []
      - {}
      responses:
        '200':
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/OpenSurvey'
          description: ''
components:
  schemas:
    DocumentSet:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        title:
          type: string
          maxLength: 200
        description:
          type: string
        documents:
          type: array
          items:
            $ref: '#/components/schemas/DocumentSetEntry'
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - documents
      - id
      - title
      - updated_at
    DocumentSetEntry:
      type: object
      properties:
        doc:
          type: string
          readOnly: true
        rank:
          type: integer
          readOnly: true
          description: Lower sorts first
        added_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - added_at
      - doc
      - rank
    DocumentSetOrder:
      type: object
      description: The set's documents, in the order they should be shown.
      properties:
        documents:
          type: array
          items:
            type: string
      required:
      - documents
    DocumentStats:
      type: object
      description: Public engagement numbers for one document.
      properties:
        doc:
          type: string
        rating_average:
          type: number
          format: double
          nullable: true
        rating_count:
          type: integer
        subscriber_count:
          type: integer
        set_count:
          type: integer
      required:
      - doc
      - rating_average
      - rating_count
      - set_count
      - subscriber_count
    KindEnum:
      enum:
      - new_rfc
      - by_status
      - obsoleted
      - rfc
      - set
      - subject
      type: string
      description: |-
        * `new_rfc` - Any new RFC
        * `by_status` - New RFC by status
        * `obsoleted` - RFC obsoleted or made historic
        * `rfc` - Changes to one specific RFC
        * `set` - Changes to anything in a document set
        * `subject` - Changes to anything carrying a subject
    MyDocument:
      type: object
      description: |-
        What one document is to the caller: their rating, their subscription,
        their sets.

        Nothing public: no average, no count, no subscriber total. Those are the
        same for everybody, so Red takes them from the data it already has for the
        route rather than from a per-caller request.
      properties:
        doc:
          type: string
        your_rating:
          type: integer
          nullable: true
        your_subscription_id:
          type: integer
          nullable: true
        your_set_ids:
          type: array
          items:
            type: string
            format: uuid
      required:
      - doc
      - your_rating
      - your_set_ids
      - your_subscription_id
    MyDocumentSet:
      type: object
      description: |-
        One of the caller's sets, without its membership.

        Deliberately not docsets.DocumentSetSerializer: that one carries the set's
        whole `documents` array, which is the thing this endpoint exists to avoid
        sending. A client drawing "which of my sets hold this document" needs every
        set the caller owns to label the rows, but it only needs membership for the
        documents it is asking about — and that arrives per document as
        `your_set_ids`. Sending both would make the payload grow with the caller's
        library rather than with the page.
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        title:
          type: string
          readOnly: true
        description:
          type: string
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - description
      - id
      - title
      - updated_at
    MyDocuments:
      type: object
      description: 'The whole response: the caller''s sets, and a row per requested
        document.'
      properties:
        sets:
          type: array
          items:
            $ref: '#/components/schemas/MyDocumentSet'
        documents:
          type: array
          items:
            $ref: '#/components/schemas/MyDocument'
      required:
      - documents
      - sets
    OpenSurvey:
      type: object
      description: |-
        One survey as Red offers it, which is as a toast.

        The fields line up with Red's Notification: title, description and url are shown,
        and slug is the string Red keys a dismissal on. documents is the addition that
        tells it where to offer this at all.
      properties:
        id:
          type: integer
          readOnly: true
        slug:
          type: string
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        title:
          type: string
          maxLength: 255
        description:
          type: string
        url:
          type: string
          readOnly: true
        documents:
          type: array
          items:
            type: string
          nullable: true
          readOnly: true
      required:
      - documents
      - id
      - slug
      - title
      - url
    PatchedDocumentSet:
      type: object
      properties:
        id:
          type: string
          format: uuid
          readOnly: true
        title:
          type: string
          maxLength: 200
        description:
          type: string
        documents:
          type: array
          items:
            $ref: '#/components/schemas/DocumentSetEntry'
          readOnly: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
    PatchedSurvey:
      type: object
      description: Full survey representation used by the management API and the builder.
      properties:
        id:
          type: integer
          readOnly: true
        slug:
          type: string
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        title:
          type: string
          maxLength: 255
        description:
          type: string
        definition: {}
        theme:
          nullable: true
        status:
          $ref: '#/components/schemas/StatusEnum'
        visibility:
          $ref: '#/components/schemas/VisibilityEnum'
        audience:
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
    PopularEntry:
      type: object
      properties:
        rfc:
          type: string
          maxLength: 32
        rank:
          type: integer
          maximum: 2147483647
          minimum: 0
          description: Lower sorts first
      required:
      - rfc
    RatingAggregate:
      type: object
      properties:
        rfc:
          type: string
        average:
          type: number
          format: double
          nullable: true
        count:
          type: integer
        your_rating:
          type: integer
          nullable: true
      required:
      - average
      - count
      - rfc
      - your_rating
    RatingWrite:
      type: object
      properties:
        value:
          type: integer
          maximum: 5
          minimum: 1
      required:
      - value
    ResponseCreate:
      type: object
      properties:
        data: {}
        meta: {}
    RetiredSubject:
      type: object
      description: |-
        A retired subject, as the only thing a retired subject is still for.

        Deliberately not the detail shape. A retired subject is not offered, does not
        appear in the vocabulary, and should not be rendered as though it were current;
        what is left is enough to redirect a link that names it. Callers tell the two
        apart by `retired`, which the live shape also carries.
      properties:
        slug:
          type: string
          readOnly: true
          description: Stable identifier used in URLs and by Red. Changing it breaks
            links that name the old one; the name is the field to edit when the wording
            is what changed.
          pattern: ^[-a-zA-Z0-9_]+$
        retired:
          type: boolean
          readOnly: true
        merged_into:
          type: string
          description: Stable identifier used in URLs and by Red. Changing it breaks
            links that name the old one; the name is the field to edit when the wording
            is what changed.
          readOnly: true
      required:
      - merged_into
      - retired
      - slug
    StatusEnum:
      enum:
      - draft
      - published
      - closed
      type: string
      description: |-
        * `draft` - Draft
        * `published` - Published
        * `closed` - Closed
    Subject:
      type: object
      description: |-
        A subject as a caller sees it, without its membership.

        The id is carried as well as the slug because subscribing names the id:
        the subscription holds a foreign key so that renaming a subject cannot
        detach its subscribers, and the id is the half of a subject's identity
        that a rename does not touch.
      properties:
        id:
          type: integer
          readOnly: true
        slug:
          type: string
          readOnly: true
          description: Stable identifier used in URLs and by Red. Changing it breaks
            links that name the old one; the name is the field to edit when the wording
            is what changed.
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          readOnly: true
          description: How the subject is shown to readers.
        description:
          type: string
          readOnly: true
          description: What belongs under this subject, for whoever curates it next
            and for a caller drawing a picker.
      required:
      - description
      - id
      - name
      - slug
    SubjectDetail:
      type: object
      description: |-
        A subject and the documents carrying it.

        Separate from the list serializer for the reason me.MyDocumentSetSerializer
        is separate from docsets.DocumentSetSerializer: a picker needs every
        subject and no membership, so sending membership in the list would make
        the payload grow with the whole catalogue rather than with the vocabulary.
      properties:
        id:
          type: integer
          readOnly: true
        slug:
          type: string
          readOnly: true
          description: Stable identifier used in URLs and by Red. Changing it breaks
            links that name the old one; the name is the field to edit when the wording
            is what changed.
          pattern: ^[-a-zA-Z0-9_]+$
        name:
          type: string
          readOnly: true
          description: How the subject is shown to readers.
        description:
          type: string
          readOnly: true
          description: What belongs under this subject, for whoever curates it next
            and for a caller drawing a picker.
        retired:
          type: boolean
          readOnly: true
        documents:
          type: array
          items:
            type: string
          readOnly: true
      required:
      - description
      - documents
      - id
      - name
      - retired
      - slug
    SubjectDetailOrRedirect:
      oneOf:
      - $ref: '#/components/schemas/SubjectDetail'
      - $ref: '#/components/schemas/RetiredSubject'
    Subscription:
      type: object
      properties:
        id:
          type: integer
          readOnly: true
        kind:
          $ref: '#/components/schemas/KindEnum'
        params: {}
        set:
          type: string
          format: uuid
          nullable: true
        subject:
          type: integer
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - id
      - kind
    Survey:
      type: object
      description: Full survey representation used by the management API and the builder.
      properties:
        id:
          type: integer
          readOnly: true
        slug:
          type: string
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        title:
          type: string
          maxLength: 255
        description:
          type: string
        definition: {}
        theme:
          nullable: true
        status:
          $ref: '#/components/schemas/StatusEnum'
        visibility:
          $ref: '#/components/schemas/VisibilityEnum'
        audience:
          nullable: true
        created_at:
          type: string
          format: date-time
          readOnly: true
        updated_at:
          type: string
          format: date-time
          readOnly: true
      required:
      - created_at
      - id
      - slug
      - title
      - updated_at
    SurveyDefinition:
      type: object
      description: Definition and theme served to the Nuxt runner.
      properties:
        slug:
          type: string
          maxLength: 100
          pattern: ^[-a-zA-Z0-9_]+$
        title:
          type: string
          maxLength: 255
        description:
          type: string
        definition: {}
        theme:
          nullable: true
        visibility:
          $ref: '#/components/schemas/VisibilityEnum'
      required:
      - slug
      - title
    VisibilityEnum:
      enum:
      - open
      - authenticated
      type: string
      description: |-
        * `open` - Open (anonymous)
        * `authenticated` - Authenticated only
  securitySchemes:
    BearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
    cookieAuth:
      type: apiKey
      in: cookie
      name: sessionid
