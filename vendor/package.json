{
  "name": "pink-vendor",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted SurveyJS UMD bundles for the Django builder and analytics pages. Not a published package; run `npm install && npm run sync` to populate static/vendor/surveyjs/.",
  "scripts": {
    "sync": "bash sync.sh",
    "build": "npm install && npm run sync"
  },
  "dependencies": {
    "survey-core": "^2.0.0",
    "survey-js-ui": "^2.0.0",
    "survey-creator-core": "^2.0.0",
    "survey-creator-js": "^2.0.0",
    "survey-analytics": "^2.0.0",
    "plotly.js-dist-min": "^2.35.0"
  }
}
