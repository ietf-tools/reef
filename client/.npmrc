# Nuxt's deep peer-dependency graph trips an npm 10.9 arborist bug
# ("Cannot read properties of null (reading 'edgesOut')") during peer
# resolution. legacy-peer-deps skips that code path and installs cleanly.
legacy-peer-deps=true

audit=false
fund=false
save-exact=true
save-prefix=""
update-notifier=false
