# Placeholder Secret so the manifests render. Do NOT commit real secrets here;
# supply them per environment via a sealed secret or the cluster's secret store.
apiVersion: v1
kind: Secret
metadata:
  name: reef-secrets
type: Opaque
stringData:
  REEF_DJANGO_SECRET_KEY: "REPLACE_ME"
  REEF_DB_PASS: "REPLACE_ME"
  REEF_OIDC_RP_CLIENT_SECRET: "REPLACE_ME"
  REEF_SURVEYJS_LICENSE_KEY: ""
