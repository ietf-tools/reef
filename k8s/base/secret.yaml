# Placeholder Secret so the manifests render. Do NOT commit real secrets here;
# supply them per environment via a sealed secret or the cluster's secret store.
apiVersion: v1
kind: Secret
metadata:
  name: pink-secrets
type: Opaque
stringData:
  PINK_DJANGO_SECRET_KEY: "REPLACE_ME"
  PINK_DB_PASS: "REPLACE_ME"
  PINK_OIDC_RP_CLIENT_SECRET: "REPLACE_ME"
  PINK_SURVEYJS_LICENSE_KEY: ""
