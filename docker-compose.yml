services:
  db:
    build:
      dockerfile: docker/db.Dockerfile
    volumes:
      - postgresdb-data:/var/lib/postgresql/data
      - .:/workspace
    restart: unless-stopped
    environment:
      - POSTGRES_DB=pink
      - POSTGRES_USER=pink
      - POSTGRES_PASSWORD=pink
      - POSTGRES_HOST_AUTH_METHOD=trust
      - DUMPFILE=

  app:
    build:
      context: .
      dockerfile: docker/app.Dockerfile
    image: pink-app
    command: sleep infinity
    volumes:
      - .:/workspace
    environment:
      - PINK_DEPLOYMENT_MODE=development
      - POSTGRES_DB=pink
      - POSTGRES_USER=pink
      - POSTGRES_PASSWORD=pink
      - POSTGRES_HOST=db
      - PINK_BROKER_URL=amqp://mq/
      # OIDC (Authentik at account.ietf.org). Set these in a local .env file.
      - PINK_OIDC_APP_SLUG=${PINK_OIDC_APP_SLUG:-pink}
      - PINK_OIDC_RP_CLIENT_ID=${PINK_OIDC_RP_CLIENT_ID:-}
      - PINK_OIDC_RP_CLIENT_SECRET=${PINK_OIDC_RP_CLIENT_SECRET:-}
      - PINK_OIDC_STAFF_GROUPS=${PINK_OIDC_STAFF_GROUPS:-}
      - PINK_SURVEYJS_LICENSE_KEY=${PINK_SURVEYJS_LICENSE_KEY:-}
      # Public client id for the Nuxt runner's browser OIDC login.
      - NUXT_PUBLIC_OIDC_CLIENT_ID=${NUXT_PUBLIC_OIDC_CLIENT_ID:-}
    depends_on:
      - db
      - mq
    extra_hosts:
      - "host.docker.internal:host-gateway"

  celery:
    image: pink-app
    command: ["/celery-init.sh"]
    volumes:
      - .:/workspace
    restart: unless-stopped
    environment:
      - PINK_DEPLOYMENT_MODE=development
      - POSTGRES_DB=pink
      - POSTGRES_USER=pink
      - POSTGRES_PASSWORD=pink
      - POSTGRES_HOST=db
      - PINK_BROKER_URL=amqp://mq/
      - CELERY_APP=pink
    depends_on:
      - db
      - mq
    extra_hosts:
      - "host.docker.internal:host-gateway"

  mailpit:
    image: axllent/mailpit:v1.27
    environment:
      - MP_DISABLE_VERSION_CHECK=true
      - MP_LABEL=PinkMail
    ports:
      - "1025:1025"
      - "8025:8025"

  mq:
    image: rabbitmq:4.1-management

  pgadmin:
    image: dpage/pgadmin4:latest
    restart: unless-stopped
    environment:
      - PGADMIN_DEFAULT_EMAIL=dev@ietf.org
      - PGADMIN_DEFAULT_PASSWORD=dev
      - PGADMIN_CONFIG_LOGIN_BANNER="Login with dev@ietf.org / dev"
      - PGADMIN_DISABLE_POSTFIX=True
      - PGADMIN_CONFIG_MASTER_PASSWORD_REQUIRED=False
      - SCRIPT_NAME=/pgadmin
    volumes:
      - ./docker/configs/pgadmin-servers.json:/pgadmin4/servers.json

volumes:
  postgresdb-data:
